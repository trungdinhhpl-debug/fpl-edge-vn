# ---- Web framework ----
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic>=2.11
pydantic-settings>=2.7

# ---- Data / DB ----
SQLAlchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2        # PostgreSQL driver (prod). SQLite works with no driver (dev).

# ---- HTTP / ingestion ----
httpx>=0.28
tenacity>=9.0               # retry with backoff for external calls

# ---- Modelling / optimization ----
numpy>=2.1
pulp>=3.0                   # MILP solver (ships with CBC binary)

# ---- Cache (optional; falls back to in-memory when REDIS_URL unset) ----
redis>=5.2

# ---- Scheduling background jobs ----
APScheduler>=3.10

# ---- Dev / test ----
pytest>=8.3
pytest-asyncio>=0.24
