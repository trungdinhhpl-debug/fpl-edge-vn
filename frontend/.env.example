# URL of the FastAPI backend (used by next.config.mjs rewrites to proxy /api).
BACKEND_URL=http://localhost:8000

# Optional: call the backend directly instead of the same-origin proxy.
# NEXT_PUBLIC_API_URL=http://localhost:8000
